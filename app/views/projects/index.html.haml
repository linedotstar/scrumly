.page-header
  %h1 Projects

%section
  %ul#projects

:javascript
  $(function() {
    // Blog is the app name
    window.router = new Scrumly.Routers.ProjectsRouter({projects: #{@projects.to_json.html_safe}});
    Backbone.history.start();
  });
